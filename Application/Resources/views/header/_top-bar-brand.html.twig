<div class="top-bar-brand">
    <button class="hamburger hamburger-squeeze mr-2"
            type="button"
            data-toggle="aside"
            aria-label="Menu"
            aria-controls="navigation">
              <span class="hamburger-box">
                <span class="hamburger-inner"></span>
              </span>
    </button>
    <a href="{{ ctx_page.get('site.url') }}">
        {#<img src="{{ ctx_page.get('site.logo') }}" height="32" alt="">#}
        {{ ctx_page.get('site.name') }}
    </a>
</div>

parameters:
    chang.packages.user.verify_email_change:
        enabled: false

imports:
    - ./services/*

framework:
    validation:
        mapping:
            paths:
                - '%chang.dir%/User/Resources/config/validation'

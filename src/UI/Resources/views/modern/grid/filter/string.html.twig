{% form_theme form '@ui/base/form/input.html.twig' %}

<div class="input-group-prepend">
    <span class="input-group-text">{{ filter.label|trans|raw }}</span>
</div>

{% if form.type is defined %}
    {{ form_widget(form.type) }}
{% endif %}

{{ form_widget(form.value, {'label': false, attr: { placeholder: 'Text here' }}) }}
